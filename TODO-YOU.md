# کارهای شما — چیزهایی که کد زدن نیست

> این فایل مال **شماست**. هیچ کار کدی اینجا نیست؛ کد در `TODO-CODE.md`
> است و آن با من.
>
> اینجا سه بخش است:
>   الف) چطور پروژه را اجرا کنید
>   ب) چطور هر بخش را تست کنید تا خودتان ببینید کار می‌کند
>   ج) کارهایی که فقط از دست شما برمی‌آید (حساب، دامنه، محتوای واقعی)

---

# چه چیزی همین حالا کار می‌کند

پیش از آنکه سراغ کارها بروید، بدانید چه تحویل گرفته‌اید:

**فروشگاه** — فهرست و جست‌وجوی محصولات با فیلتر، صفحه‌ی محصول، سبد
خرید (مهمان و کاربر)، تسویه‌حساب کامل، کد تخفیف، علاقه‌مندی، مقایسه‌ی
محصول، نظر و امتیاز، وبلاگ، تیکت پشتیبانی، پروفایل و آدرس‌ها، بازیابی
رمز عبور، **اعلان‌های درون‌برنامه‌ای** (زنگوله در هدر) و **فاکتور
قابل چاپ** سفارش.

**پنل مدیریت** — محصولات، دسته‌ها، برندها، سفارش‌ها، مشتریان، نظرات،
تیکت‌ها، کدهای تخفیف، مقالات، بنرهای صفحه‌ی اصلی، صندوق پیام‌های تماس،
و تنظیمات فروشگاه.

**زیرساخت** — دوزبانه‌ی کامل (فارسی راست‌چین، انگلیسی چپ‌چین)، تم روشن
و تاریک، ریسپانسیو، **۲۲۵ تست خودکار** بک‌اند، ۲۲ سوئیت تست مرورگری،
تحلیل ایستا با صفر خطا، تست خودکار روی گیت‌هاب، و **مستندات ۱۲۴
اندپوینت API** که خودش را از روی کد می‌سازد.

⚠️ **چیزی که هنوز واقعی نیست:** پرداخت با درگاه شبیه‌سازی‌شده است و
ایمیل‌ها در فایل لاگ می‌نشینند نه صندوق واقعی. هر دو منتظر حساب‌هایی
هستند که در بخش (ج) آمده — کدشان آماده است.

---

# الف) اجرای پروژه

## اجرای عادی

روی `run.bat` دابل‌کلیک کنید. تمام.

اگر ترجیح می‌دهید از ترمینال:

```
run.bat
```

این کار را می‌کند: پیش‌نیازها را بررسی می‌کند، پورت‌های اشغال را آزاد
می‌کند، هر دو سرور را بالا می‌آورد و منتظر می‌ماند تا واقعاً سالم شوند.

وقتی بالا آمد:

| چه چیزی | آدرس |
|---------|------|
| فروشگاه | http://127.0.0.1:3100 |
| پنل ادمین | http://127.0.0.1:3100/fa/admin |
| API | http://127.0.0.1:8100/api/v1 |

## کلیدهای دیگر

| فرمان | کار |
|-------|-----|
| `run.bat` | اجرای عادی |
| `run.bat -Fresh` | دیتابیس را از صفر می‌سازد و داده‌ی نمونه می‌ریزد |
| `run.bat -Prod` | فرانت را در حالت تولید اجرا می‌کند (سرعت واقعی) |
| `run.bat -Stop` | سرورها و کارگر صف را می‌بندد |
| `run.bat -Force` | پورت اشغال را بدون پرسیدن آزاد می‌کند |

⚠️ **`-Fresh` همه‌ی داده را پاک می‌کند.** اگر محصول یا سفارش دستی
ساخته‌اید، از بین می‌رود. برای تست از صفر عالی است، برای ادامه‌ی کار نه.

## حساب‌های آماده

| نقش | ایمیل | رمز |
|-----|-------|-----|
| مدیر کل | `admin@demo.dev` | `password` |
| مدیر فروشگاه | `manager@demo.dev` | `password` |
| مشتری | `user@demo.dev` | `password` |

⚠️ این‌ها فقط برای محیط محلی‌اند. پیش از انتشار روی اینترنت حتماً
رمزها را عوض کنید — در بخش (ج) دوباره یادآوری شده.

## وقتی چیزی خراب شد

اول لاگ را ببینید، حدس نزنید:

```
type .logs\web.error.log
type .logs\api.error.log
```

**دو مشکل رایج که خودم خورده‌ام:**

۱. **صفحه بالا می‌آید ولی دکمه‌ها کار نمی‌کنند.** یعنی دیسک پر شده و
   نکست نتوانسته کش بنویسد. خطا فقط در `web.error.log` است، در مرورگر
   هیچ نشانه‌ای ندارد. راه‌حل:

   ```
   powershell -ExecutionPolicy Bypass -File scripts\clean-cache.ps1
   ```

۲. **همه‌ی صفحه‌ها ۵۰۰ می‌دهند و از `[turbopack]_runtime.js` حرف می‌زنند.**
   یعنی کش وسط اجرای سرور پاک شده. سرور را ببندید (`run.bat -Stop`)،
   دوباره اجرا کنید.

## فضای دیسک

پروژه با کش بیلد راحت چند گیگ می‌گیرد. اگر فضا کم شد:

```
powershell -ExecutionPolicy Bypass -File scripts\clean-cache.ps1
```

برای پاکسازی عمیق‌تر (مرورگر تست را هم پاک می‌کند، بعداً دوباره دانلود
می‌شود):

```
powershell -ExecutionPolicy Bypass -File scripts\clean-cache.ps1 -DeepClean
```

---

# ب) چطور خودتان تست کنید

سه لایه تست هست. از سریع به کند:

## لایه ۱ — تست بک‌اند (۱۶ ثانیه، سرور لازم ندارد)

```
cd nextstore-api
..\tools\php\php.exe artisan test
```

باید همه سبز باشند. این‌ها منطق تجاری را می‌سنجند: قیمت، تخفیف کوپن،
موجودی، دسترسی‌ها.

## لایه ۲ — تست سرتاسری با مرورگر واقعی

⚠️ **اول باید `run.bat` اجرا شده باشد.** این تست‌ها مرورگر واقعی باز
می‌کنند و مثل یک کاربر کلیک می‌کنند.

```
cd nextstore-web
pnpm test:e2e                 # هر ۲۲ سوئیت، پشت سر هم (حدود ۲۰ دقیقه)
pnpm test:e2e checkout        # فقط آن‌هایی که «checkout» در نامشان است
```

⚠️ **وسط اجرا کش را پاک نکنید.** اگر `clean-cache.ps1` را در حین اجرا
بزنید، سرور وب می‌میرد و **همه‌ی** تست‌ها شکست می‌خورند — شکستی که
شبیه صدها باگ به نظر می‌رسد ولی هیچ‌کدام واقعی نیست. (خودم یک بار
همین اشتباه را کردم.)

در پایان، گزارشی می‌دهد که کدام سوئیت‌ها شکست خورده‌اند و چرا. اجرا با
اولین شکست متوقف نمی‌شود، پس یک بار اجرا همه‌ی مشکلات را نشان می‌دهد.

هر فایل یک بخش را می‌سنجد. مهم‌ترین‌ها:

| فایل | چه چیزی را می‌سنجد |
|------|--------------------|
| `e2e-checkout.mjs` | کل مسیر خرید: سبد → آدرس → پرداخت → سفارش |
| `e2e-coupons.mjs` | اعمال کوپن و همه‌ی حالت‌های خطایش |
| `e2e-filters.mjs` | فیلتر و مرتب‌سازی محصولات |
| `e2e-reviews.mjs` | ثبت نظر و تأیید ادمین |
| `e2e-tickets.mjs` | پشتیبانی |
| `e2e-admin-crud.mjs` | ساخت/ویرایش/حذف محصول در پنل |
| `e2e-profile.mjs` | ثبت‌نام، ورود، ویرایش پروفایل |
| `e2e-compare.mjs` | مقایسه‌ی محصولات |
| `e2e-contact.mjs` | فرم تماس و صندوق پیام‌های پنل |
| `e2e-banners.mjs` | بنرهای صفحه‌ی اصلی و زمان‌بندی‌شان |
| `e2e-password-reset.mjs` | بازیابی رمز از ایمیل تا ورود با رمز تازه |

فهرست کامل: `dir nextstore-web\scripts\e2e-*.mjs`

## لایه ۲.۵ — بررسی‌های سریع بدون سرور

```
cd nextstore-web
pnpm typecheck                # خطاهای TypeScript
pnpm lint                     # ESLint
pnpm check:i18n               # برابری کلیدهای فارسی و انگلیسی

cd ..
extstore-api
..	ools\php\php.exe ..	ools\composer.phar analyse    # تحلیل ایستا
```

⚠️ `check:i18n` مهم‌تر از آن است که به نظر می‌رسد: اگر کلیدی فقط به
فارسی اضافه شود، نسخه‌ی فارسی درست کار می‌کند و **هیچ خطایی هم
نمی‌دهد** — تا وقتی کسی سایت را انگلیسی باز کند.

هر تست آخرش می‌گوید `N passed, M failed`. اگر چیزی قرمز شد، خط بالایش
می‌گوید کجا.

## لایه ۳ — بازبینی چشمی

```
cd nextstore-web
pnpm shots
```

عکس‌ها در `nextstore-web\.shots\` می‌افتد. **واقعاً نگاهشان کنید.**
تست‌ها می‌گویند دکمه کار می‌کند؛ نمی‌گویند دکمه زشت است یا متن از کادر
بیرون زده.

## تستی که فقط شما می‌توانید بکنید

هیچ تست خودکاری این‌ها را نمی‌گیرد — با موبایل واقعی خودتان چک کنید:

- [ ] سایت روی گوشی واقعی (نه فقط حالت موبایل مرورگر) درست است؟
- [ ] متن فارسی همه‌جا راست‌چین است و انگلیسی چپ‌چین؟
- [ ] تم تاریک را روشن کنید — جایی متن روی پس‌زمینه‌ی هم‌رنگ گم نمی‌شود؟
- [ ] یک خرید کامل انجام دهید، از اول تا آخر، بدون عجله. جایی گیج شدید؟
- [ ] یک نظر ثبت کنید و از پنل تأییدش کنید.
- [ ] یک تیکت بزنید و از پنل جوابش را بدهید.
      ⚠️ بعدش زنگوله‌ی بالای صفحه باید نشان قرمز بگیرد — اعلان پاسخ
      پشتیبانی.
- [ ] از صفحه‌ی یک سفارش، دکمه‌ی «فاکتور» را بزنید و Ctrl+P کنید.
      روی کاغذ نباید هدر، فوتر یا منو دیده شود — فقط خود فاکتور.
- [ ] از پنل، بنرهای صفحه‌ی اصلی را عوض کنید و ببینید بلافاصله در
      فروشگاه اثر می‌کند.

---

# ج) کارهایی که فقط از دست شما برمی‌آید

## ج-۱ · محتوای واقعی

پروژه الان با داده‌ی نمونه پر شده. برای اینکه واقعی به نظر برسد:

- [ ] **عکس محصول واقعی.** مهم‌ترین کار این فهرست. عکس بی‌کیفیت یا
      تکراری، حرفه‌ای‌ترین کد را هم ارزان نشان می‌دهد.
- [ ] **متن «درباره ما»** — چند خط واقعی، نه متن پرکننده.
- [ ] **متن قوانین و حریم خصوصی.** اگر واقعاً می‌فروشید، این‌ها را از
      روی نمونه ننویسید؛ متن حقوقی درست بگیرید.
- [ ] **دو سه پست وبلاگ واقعی.**
- [ ] **لوگو.**

## ج-۲ · حساب‌های سرویس

هیچ‌کدام کد نمی‌خواهند؛ ثبت‌نام و گرفتن کلید است. کلیدها را که گرفتید
به من بدهید تا وصلشان کنم.

- [ ] **درگاه پرداخت** (زرین‌پال، آیدی‌پی، ...). نیاز به نماد اعتماد و
      مدارک هویتی دارد و چند روز طول می‌کشد — زودتر شروع کنید.
- [ ] **پنل پیامک** (کاوه‌نگار، ملی‌پیامک) برای کد تأیید و اطلاع سفارش.
- [ ] **سرویس ایمیل** (میل‌گان، سندگرید، یا SMTP هاست).

      **کد آماده است.** سه ایمیل نوشته شده و در صف می‌روند: خوش‌آمد پس
      از ثبت‌نام، تأیید سفارش، و بازیابی رمز عبور.

      الان با `MAIL_MAILER=log` کار می‌کنند: به‌جای ارسال، متنشان در
      `nextstore-api/storage/logs/laravel.log` نوشته می‌شود. برای دیدن
      آخرین ایمیل:

      ```
      cd nextstore-api
      type storage\logs\laravel.log
      ```

      وقتی حساب گرفتید، فقط این خط‌ها را در `nextstore-api/.env` عوض
      کنید و تمام — هیچ کد دیگری لازم نیست:

      ```
      MAIL_MAILER=smtp
      MAIL_HOST=smtp.example.com
      MAIL_PORT=587
      MAIL_USERNAME=...
      MAIL_PASSWORD=...
      MAIL_FROM_ADDRESS="noreply@yourdomain.com"
      MAIL_FROM_NAME="نام فروشگاه شما"
      ```

      ⚠️ **کارگر صف باید در حال اجرا باشد.** `run.bat` خودش اجرایش
      می‌کند، ولی روی سرور واقعی باید با `supervisor` یا معادلش
      همیشه زنده نگه داشته شود:

      ```
      php artisan queue:work --tries=3
      ```

      بدون آن، ایمیل‌ها در جدول `jobs` می‌مانند و **هیچ خطایی هم داده
      نمی‌شود** — کاربر پیام موفقیت می‌بیند و ایمیل هرگز نمی‌رسد.
- [ ] **حساب ابری برای عکس‌ها** اگر عکس‌ها زیاد شد (آروان، لیارا).

## ج-۳ · انتشار

- [ ] **دامنه بخرید.**
- [ ] **هاست یا سرور بگیرید.** لاراول به PHP نیاز دارد؛ هاست اشتراکی
      معمولی جواب می‌دهد ولی سرور مجازی راحت‌تر است.
- [ ] **گواهی SSL** — تقریباً همه‌ی هاست‌ها رایگان می‌دهند
      (Let's Encrypt). بدون آن مرورگر به کاربر هشدار می‌دهد.
- [ ] **مخزن گیت‌هاب بسازید** و پروژه را پوش کنید. مخزن محلی آماده است
      (شاخه‌های `main` و `develop` ساخته شده‌اند).

      ⚠️ به‌محض اولین push، تست‌های خودکار روی گیت‌هاب اجرا می‌شوند
      (`.github/workflows/ci.yml`). هیچ تنظیمی لازم ندارید — فقط
      مخزن را بسازید و پوش کنید. اگر تیک سبز نخورد، همان‌جا می‌گوید
      کجا مشکل دارد.

## ج-۴ · امنیت پیش از انتشار

⚠️ **این بخش را رد نکنید.**

- [ ] رمز هر سه حساب نمونه را عوض کنید یا حذفشان کنید.
- [ ] `APP_DEBUG=false` در `.env` سرور. اگر `true` بماند، متن خطا
      مسیر فایل‌ها و گاهی مقدار متغیرها را به بازدیدکننده نشان می‌دهد.
- [ ] `APP_KEY` سرور باید **متفاوت** از محلی باشد.
- [ ] فایل `.env` هرگز نباید در گیت‌هاب باشد. (الان در `.gitignore` هست،
      ولی موقع انتقال دستی حواستان باشد.)
- [ ] از دیتابیس پشتیبان بگیرید — قبل از هر تغییر بزرگ.

## ج-۵ · معرفی پروژه

اگر این پروژه نمونه‌کار است:

- [ ] **ویدیوی کوتاه دمو** (دو تا سه دقیقه): یک خرید کامل + یک نگاه به
      پنل. بیشتر آدم‌ها ویدیو را می‌بینند و کد را نه.
- [ ] **چند اسکرین‌شات در `README.md`.**
      با `pnpm shots` گرفته می‌شوند و در `nextstore-web/.shots/` می‌افتند.

- [ ] **تصویر پیش‌نمایش شبکه‌های اجتماعی را بازبینی کنید.**
      دو فایل آماده است: `nextstore-web/public/og-fa.png` و `og-en.png`.
      وقتی لینک سایت را در تلگرام یا لینکدین می‌فرستید، همین‌ها نشان
      داده می‌شوند. اگر لوگو یا نام فروشگاه را عوض کردید، با
      `node scripts/build-og.mjs` دوباره بسازیدشان.
- [ ] **لینک نسخه‌ی آنلاین** در بالای README.

---

## وقتی چیزی نامفهوم بود

بنویسید کجا گیر کردید و چه دیدید. اگر خطا بود، متن خطا را از
`.logs\web.error.log` یا `.logs\api.error.log` بردارید — بدون آن
باید حدس بزنم.
