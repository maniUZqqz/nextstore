# فهرست کارهای کدی

> این فایل مال **کلاد** است. هر چیزی که تایپ کردن کد است اینجاست.
> کارهایی که کد نیست (دامنه، هاست، حساب درگاه، عکس واقعی، ویدیو)
> در `TODO-YOU.md` جدا شده.
>
> علامت‌ها:  `[ ]` انجام‌نشده · `[x]` انجام‌شده · `[~]` در حال انجام

---

## دسته ۱ — ناهماهنگی داده: چیزی که باید داینامیک باشد و نیست

بعد از ممیزی جریان داده در ۸ شهریور پیدا شد. این‌ها باگ‌اند نه قابلیت:
کد نوشته شده، ولی داده‌اش از جای اشتباه می‌آید.

- [x] **۱.۱ صفحه‌ی تماس از تنظیمات بخواند**
      `contact/page.tsx` ایمیل `support@nextstore.dev` را هاردکد دارد و
      تلفن/آدرس را از فایل ترجمه می‌خواند. هدر و فوتر همین‌ها را از
      `settings` می‌خوانند.
      نتیجه: ادمین شماره را عوض می‌کند، فوتر عوض می‌شود، صفحه‌ی تماس نه.
      کلیدها همین حالا وجود دارند: `contact_phone` `contact_email`
      `contact_address` `support_hours`.

- [x] **۱.۲ فرم تماس واقعاً چیزی بفرستد**
      بررسی شود که ارسال فرم به تیکت تبدیل می‌شود یا در خلأ می‌افتد.

- [x] **۱.۳ جدول `banners` + مدیریت بنر در پنل**
      `HeroSlider` سه اسلاید و `PromoBanners` سه بنر را در کد دارند.
      نامک دسته‌ها هم هاردکد است — با حذف دسته، لینک مرده می‌شود.
      نیاز دارد به: مهاجرت، مدل، سیدر (همین سه اسلاید فعلی به‌عنوان
      داده‌ی اولیه تا صفحه خالی نشود)، API عمومی، CRUD ادمین،
      و اتصال هر دو کامپوننت.

- [x] **۱.۴ صفحات محتوایی — بررسی شد و تصمیم گرفته شد**

      متن این صفحات در `src/content/` است، نه در `messages/*.json` و نه
      در دیتابیس. این **تصمیم درست است و نگه داشته شد**، به دو دلیل که
      در `src/content/README.md` مستند شده‌اند:

      ۱. `next-intl` تمام پیام‌ها را در payload هر صفحه جاسازی می‌کند.
         این ۵۰ کیلوبایت متن روی صفحه‌ی اصلی و سبد خرید هم فرستاده
         می‌شد بدون اینکه خوانده شود.

      ۲. متن حقوقی (قوانین، حریم خصوصی) باید تاریخچه‌ی گیت داشته باشد.
         «چه کسی، کِی، چه بندی را عوض کرد» در دیتابیس گم می‌شود.

      **اما یک باگ واقعی داخلش بود که رفع شد:** مبالغ ارسال در متن
      نوشته شده بودند و با `config/shop.php` — که منطق پرداخت از آن
      می‌خواند — دو منبع حقیقت می‌ساختند. حالا درج می‌شوند.

---

## دسته ۲ — قابلیت‌های ناتمام

- [x] **۲.۱ بازیابی رمز عبور**

      لینک از روز اول در صفحه‌ی ورود بود، `robots.ts` فهرستش کرده بود و
      سؤالات متداول هم به مشتری می‌گفت از آن استفاده کند — ولی صفحه
      وجود نداشت و کاربر به ۴۰۴ می‌رسید.

      حالا: دو صفحه، دو مسیر API با سطل نرخ جدا، ایمیل دوزبانه با پیوند
      به فرانت، توکن یک‌بارمصرف که همه‌ی نشست‌ها را هم می‌بندد.

- [x] **۲.۲ اعلان‌ها**
      جدول، زنگوله با نشان عددی و کشوی پیش‌نمایش، صفحه‌ی کامل با تب و
      صفحه‌بندی. چهار رویداد: وضعیت سفارش، پاسخ پشتیبانی، تأیید و رد نظر.
      متن **ذخیره نمی‌شود** — از نوع رویداد ساخته می‌شود تا با عوض شدن
      زبان سایت، اعلان‌های قدیمی هم ترجمه شوند.

- [x] **۲.۳ فاکتور سفارش**
      صفحه‌ی چاپ‌بهینه با دکمه در صفحه‌ی سفارش. کاربر با Ctrl+P خروجی
      PDF می‌گیرد.

      ⚠️ عمداً کتابخانه‌ی PDF سمت سرور استفاده **نشد**: `dompdf` حروف
      فارسی را به هم نمی‌چسباند و راست‌به‌چپ را وارونه می‌چیند. مرورگر
      همان چیزی را چاپ می‌کند که روی صفحه دیده می‌شود — با فونت،
      اتصال حروف و اعداد فارسی درست.

- [x] **۲.۴ صف ایمیل**
      سه ایمیل در صف: خوش‌آمد، تأیید سفارش، بازیابی رمز. `run.ps1`
      کارگر صف را اجرا و با `-Stop` می‌بندد.
      ⚠️ با `MAIL_MAILER=log` کامل کار می‌کند؛ سرویس واقعی کار شماست و
      در `TODO-YOU.md` دقیقاً نوشته شده چه چیزی را کجا عوض کنید.

---

## دسته ۳ — کیفیت کد و ابزار

- [x] **۳.۱ PHPStan** — نصب شد، پیکربندی شد، از ۴۱۷ خطا به **صفر** رسید.
- [x] **۳.۲ اسکریپت‌های npm برای تست‌ها**
      `pnpm test:e2e` هر ۲۲ سوئیت را سریال اجرا می‌کند و در پایان
      گزارش می‌دهد؛ `pnpm test:e2e <کلمه>` فیلتر می‌کند.
      `pnpm typecheck` هم اضافه شد.
- [x] **۳.۳ CI/CD** — سه job موازی روی هر push و PR:
      لاراول (Pint · PHPStan · Pest) · نکست (tsc · eslint · build) ·
      برابری ترجمه‌ها.
- [x] **۳.۴ تصاویر OG**
      متادیتا `summary_large_image` وعده می‌داد ولی تصویری نمی‌داد —
      کارت خالی. حالا دو تصویر دوزبانه در `public/` و بازگشت به
      آن‌ها وقتی محصول یا مقاله عکس ندارد.
- [x] **۳.۵ مستندات API**
      `php artisan docs:api` هر ۱۲۴ اندپوینت را از روی جدول مسیرها و
      docblock کنترلرها مستند می‌کند. CI بررسی می‌کند که کهنه نشده باشد.
      ⚠️ Scribe استفاده **نشد**: توضیحاتش را از انوتیشن مخصوص خودش
      می‌خواند، یعنی همان چیزی که در docblock هست دوباره و به شکل دیگری
      نوشته می‌شود — و از آن لحظه دو متن داریم که از هم دور می‌افتند.

---

## دسته ۴ — بازبینی نهایی پیش از تحویل

- [x] **۴.۱ اجرای کامل هر ۲۲ سوئیت e2e** — اجرا شد، ۹ سوئیت شکست، هر ۹ رفع شد.

      دو تای آن‌ها باگ واقعی محصول بودند، نه نقص تست:

      - `AdminPagination` کلید ترجمه‌اش را از فضای `admin` می‌گرفت ولی در
        `components/account/` هم استفاده می‌شد. بسته‌ی پیام صفحه‌های
        فروشگاه عمداً فضای `admin` را ندارد، پس **صفحه‌ی اعلان‌ها و
        تیکت‌های کاربر از صفحه‌ی دوم به بعد با MISSING_MESSAGE می‌افتاد**.
        کلید به `common` منتقل شد و `pnpm check:i18n` حالا این نشتی را
        هم می‌گیرد.
      - صفحه‌ی محصول همان کلید «سفارش بالای {amount} {currency}» را
        **بدون پارامتر** صدا می‌زد: خطای FORMATTING_ERROR در کنسول هر
        صفحه‌ی محصول، و نمایش خودِ `{amount}` به‌جای مبلغ. مقدار حالا از
        یک کمکی مشترک (`freeShippingValues`) می‌آید که هر سه مصرف‌کننده
        از آن استفاده می‌کنند.

      بقیه سه دسته بودند: مسابقه‌ی زمانی (انتظار ثابت به‌جای شرط —
      `scripts/lib/wait.mjs` اضافه شد)، آلودگی داده بین اجراها (سقف نرخ
      فرم تماس و سهمیه‌ی کوپن)، و یک `STAMP` تعریف‌نشده.

- [x] **۴.۲ اجرای Pest کامل** — ۲۲۸ تست، همه سبز.
- [x] **۴.۳ `next build` بدون خطا و هشدار** — اجرا شد، تمیز.
- [ ] **۴.۴ بازبینی چشمی** با `pnpm shots` روی موبایل و دسکتاپ، هر دو تم،
      هر دو زبان.
- [x] **۴.۵ برابری کلیدهای ترجمه** — `pnpm check:i18n` + یک job در CI.
      ۱۲۲۸ کلید در هر دو زبان، به‌علاوه‌ی نگهبان نشتی فضای `admin`.

---

## آنچه از دل رفع e2e بیرون آمد

اینها در تودو نبودند و حین کار لازم شدند:

- **`scripts/lib/wait.mjs`** — انتظار شرطی به‌جای `waitForTimeout`. زمان
  ثابت روی ماشین کند کم می‌آید و شکستش شبیه باگ محصول به نظر می‌رسد؛
  روی ماشین سریع هم وقت تلف می‌کند.
- **`php artisan e2e:reset-limits`** — صفر کردن شمارنده‌ی سقف نرخ پیش از
  اجرای تست. **هیچ سقفی کم نشد**؛ فقط شمارنده مثل هر داده‌ی بازمانده‌ی
  دیگری پاک می‌شود. در محیط غیر local اجرا نمی‌شود.
- **`tests/Feature/Support/RateLimitTest.php`** — تضمین اینکه هر سقف سطل
  جدا دارد. الان درست است ولی *تصادفی* درست است (لاراول خودش کلید را با
  attempts/decay متمایز می‌کند)، و این تست همان فرض را میخکوب می‌کند.
- **بررسی سلامت پیش از اجرا در `run-e2e.mjs`** — شش صفحه‌ی نماینده باید
  ۲۰۰ بدهند وگرنه اجرا با پیام روشن متوقف می‌شود. یک بار ماژول
  کامپایل‌شده‌ی دِو سرور خراب شده بود و دو سوئیت با شکست‌های بی‌ربط
  افتادند؛ کد سالم بود.
