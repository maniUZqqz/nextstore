# تطبیق پروژه با رودمپ

> ساخته‌شده در ۱۹ شهریور ۱۴۰۵ · مقایسه‌ی `rodmap.md` با کدی که واقعاً وجود دارد.
>
> رودمپ یک **مشخصات ۱۴ هفته‌ای** است. این فایل نمی‌گوید پروژه بد است؛
> می‌گوید کجای آن مشخصات پیاده شده، کجا عمداً جور دیگری تصمیم گرفته
> شده، و کجا واقعاً حفره است.

---

## ۱. اعداد

| سنجه | رودمپ | پروژه | |
|------|-------|-------|---|
| جدول دیتابیس | ۳۵+ | ۳۳ | ۹۴٪ |
| اندپوینت API | ۹۰+ | ۱۲۶ | **۱۴۰٪** |
| صفحه‌ی فرانت | ۷۱ | ۶۱ | ۸۶٪ |
| کامپوننت React | ۱۵۰+ | ۹۷ | ۶۵٪ |
| کلاس بک‌اند | ۲۰۰+ | ۱۴۳ | ۷۲٪ |

عدد کامپوننت گمراه‌کننده است: رودمپ برای هر حالت یک کامپوننت جدا
شمرده (`ProductCardSkeleton`، `ProductCardCompact`…) در حالی که اینجا
یک کامپوننت با prop همان کار را می‌کند. اندپوینت‌ها هم بیشترند چون
عملیات ادمین ریزتر تفکیک شده‌اند.

---

## ۲. آنچه کامل پیاده شده

**زیرساخت** — دوزبانگی با ستون JSON، RTL/LTR، تم تاریک/روشن بدون FOUC،
ریسپانسیو، لایه‌ی API با مدیریت خطای متمرکز، کش با برچسب و باطل‌سازی
هدفمند از پنل.

**فروشگاه** — صفحه‌ی اصلی داینامیک (بنر از دیتابیس)، فهرست و فیلتر و
مرتب‌سازی محصولات، صفحه‌ی محصول با JSON-LD، دسته و برند، جستجو، سبد
مهمان و کاربر با **ادغام هنگام ورود**، مقایسه، علاقه‌مندی، کد تخفیف با
همه‌ی محدودیت‌ها (درصدی/ثابت، سقف تخفیف، حداقل سبد، سقف کل، سقف هر
کاربر، بازه‌ی زمانی)، تسویه، پرداخت با درگاه شبیه‌سازی‌شده، فاکتور
قابل چاپ.

**حساب کاربری** — داشبورد، سفارش‌ها با تایم‌لاین وضعیت، لغو سفارش،
آدرس‌ها، علاقه‌مندی، نظرات من، تیکت پشتیبانی (کامل، با بستن و
بازکردن)، اعلان‌ها، پروفایل، امنیت با **فهرست نشست‌های فعال و خروج از
هر کدام**.

**پنل مدیریت** — داشبورد، محصولات (CRUD + تغییر سریع موجودی)، دسته‌ها
با ترتیب‌دهی، برندها، سفارش‌ها با تغییر وضعیت و کد رهگیری، مشتریان با
پروفایل و تاریخچه، کدهای تخفیف، بنرها، مقالات، نظرات با تأیید/رد،
تیکت‌ها، صندوق پیام تماس، تنظیمات.

**کیفیت** — بدون `any`، بدون `console.log`، بدون `dd()`؛ Pint و ESLint
تمیز؛ PHPStan صفر خطا؛ ۲۴۰ تست Pest؛ ۲۲ سوئیت e2e با مرورگر واقعی؛ CI
سه‌جابی؛ مستندات API از خود کد ساخته می‌شود.

---

## ۳. آنچه عمداً جور دیگری تصمیم گرفته شد

اینها حفره نیستند؛ انتخاب‌اند و هر کدام در کد مستند شده‌اند.

| رودمپ | این پروژه | چرا |
|-------|-----------|-----|
| جدول `pages` برای صفحات ایستا | `src/content/*.ts` | `next-intl` همه‌ی پیام‌ها را در payload هر صفحه جاسازی می‌کند؛ متن «درباره‌ی ما» را به هر بازدیدکننده‌ی صفحه‌ی محصول هم می‌فرستاد. مستند در `src/content/README.md`. |
| جدول `shipping_methods` | `config/shop.php` + enum | نرخ‌ها ثابت‌اند و در پنل عوض نمی‌شوند. جدول برای داده‌ای که تغییر نمی‌کند، پیچیدگی بی‌جاست. |
| جدول `provinces`/`cities` | متن آزاد در آدرس | فهرست کامل شهرهای ایران چند هزار ردیف است و برای نمونه‌کار ارزش افزوده‌ای ندارد. |
| `/compare` سمت سرور | Zustand سمت کلاینت | مقایسه حالت گذراست، نه داده‌ی کاربر. ذخیره‌اش روی سرور یعنی یک درخواست اضافه برای چیزی که با بستن تب از بین می‌رود. |
| `/search` و `/search/suggestions` جدا | `/products?q=` | همان کوئری با همان فیلترهاست؛ اندپوینت دوم فقط کد تکراری می‌شد. |
| تسویه‌ی سه‌صفحه‌ای | یک جریان + صفحه‌ی درگاه | مراحل در همان صفحه‌اند. صفحه‌ی جدا برای هر مرحله یعنی سه بار رفت‌وبرگشت سرور برای کاری که با یک state انجام می‌شود. |
| درگاه پرداخت واقعی | درگاه شبیه‌سازی‌شده | نمونه‌کار نمی‌تواند حساب درگاه بانکی داشته باشد. قرارداد کامل شبیه‌سازی شده (موفق/ناموفق/انصراف). در `TODO-YOU.md` توضیح داده شده. |
| Spatie roles/permissions | ستون `role` روی کاربر | سه نقش داریم: مدیر، مدیر محتوا، مشتری. جدول‌های چندگانه برای سه مقدار، پیچیدگی بی‌مصرف است. |

---

## ۴. حفره‌های واقعی

اینها در رودمپ هستند و در پروژه نیستند — و **دلیل طراحی ندارند**،
فقط ساخته نشده‌اند.

### بزرگ (هر کدام چند روز کار)

| # | چه چیزی | اثرش |
|---|---------|------|
| ۱ | **تنوع محصول** (`product_variants`، `attributes`) | محصول رنگ و سایز ندارد. برای فروشگاه پوشاک یا موبایل، این هسته‌ی کار است. سه صفحه‌ی پنل و فیلتر `attributes` هم به آن وابسته‌اند. |
| ۲ | **کیف پول** | صفحه‌ی ۴۲ پنل کاربری و سه اندپوینت. |
| ۳ | **گزارش‌ها** (فروش، محصولات، مشتریان) + خروجی Excel | سه صفحه‌ی پنل. داشبورد آمار دارد ولی گزارش بازه‌ای ندارد. |
| ۴ | **مدیریت رسانه** و **آپلود تصویر محصول** | ⚠️ این یکی فقط «نبودن قابلیت» نیست: مدیر محصول تازه می‌سازد و **هیچ راهی برای گذاشتن عکس ندارد**. فرم محصول فیلد تصویر ندارد. |
| ۵ | **ورود با OTP و گوگل** | صفحه‌ی ۲۶ و چهار اندپوینت. |

### کوچک

| # | چه چیزی | وضعیت |
|---|---------|-------|
| ۶ | **خبرنامه** | ✅ **همین حالا ساخته شد** — جدول، مدل، دو اندپوینت، ۱۲ تست، و فرم پای صفحه که پیش‌تر ساختگی بود. |
| ۷ | **تأیید ایمیل** | ✅ **همین حالا ساخته شد** — اعلان صف‌دار با پیوند امضاشده، دو اندپوینت، صفحه‌ی `/verify-email`، دکمه‌ی «ارسال دوباره» در پروفایل، ۱۳ تست. |
| ۸ | نظرات بلاگ | مقاله نظر ندارد. |
| ۹ | عملیات گروهی روی محصولات | حذف/انتشار دسته‌جمعی نیست. |
| ۱۰ | `/categories/tree` و `/categories/{slug}/filters` | فیلترها از خود فهرست محصول می‌آیند. |
| ۱۱ | آپلود آواتار، تنظیم زبان/تم پیش‌فرض در پروفایل | تم و زبان در مرورگر ذخیره می‌شوند، نه روی حساب. |
| ۱۲ | اسکرین‌شات در README | رودمپ صریحاً خواسته. عکس‌ها با `pnpm shots` ساخته می‌شوند ولی در README نیامده‌اند. |
| ۱۳ | PHPStan سطح ۶+ | سطح ۵ با صفر خطا. سطح ۶ دقیقاً **۱۰۶** خطا می‌دهد و همه از خانواده‌ی `missingType.*`اند — یعنی annotation ناقص، نه باگ. بستنش حدود ۴۰ فایل ویرایش مکانیکی است. |

---

## ۵. چک‌لیست تحویل رودمپ (بخش ۱۵)

| بند | وضعیت |
|-----|-------|
| ثبت‌نام و ورود با ایمیل | ✅ |
| ورود با موبایل / OTP / گوگل | ❌ |
| مرور، فیلتر، مرتب‌سازی، جستجو | ✅ |
| جزئیات محصول **با تنوع** | ⚠️ صفحه کامل است، تنوع نیست |
| سبد مهمان و کاربر + ادغام | ✅ |
| کد تخفیف با همه‌ی محدودیت‌ها | ✅ |
| تسویه | ⚠️ یک‌مرحله‌ای به‌جای سه‌مرحله‌ای (تصمیم) |
| پرداخت با درگاه واقعی تستی | ⚠️ شبیه‌سازی‌شده (تصمیم) |
| پنل کاربری ۱۲ صفحه | ✅ ۱۲ از ۱۳ (کیف پول نیست) |
| پنل ادمین ۲۸ صفحه | ⚠️ ۱۸ از ۲۶ |
| نظر و امتیاز با تأیید | ✅ |
| بلاگ با دسته | ✅ (بدون نظر) |
| تیکت پشتیبانی | ✅ |
| کیف پول | ❌ |
| **دوزبانه** — همه‌ی صفحات، بدون متن هاردکد، RTL سالم، اعداد محلی، خطاهای API دوزبانه، hreflang، حفظ مسیر هنگام تعویض زبان | ✅ همه |
| ایمیل به زبان کاربر | ✅ |
| **تم** — هر دو تم، بدون FOUC، ماندگار، حالت سیستمی، کنتراست | ✅ |
| **ریسپانسیو** — بدون اسکرول افقی، ناحیه‌ی لمس، جدول‌های موبایل | ✅ |
| **کیفیت** — هدر توضیحی، بدون `any`، ESLint و Pint، بدون `console.log` | ✅ |
| PHPStan سطح ۶+ | ⚠️ سطح ۵ (سطح ۶ = ۱۰۶ خطای annotation، بدون باگ) |
| README با اسکرین‌شات | ⚠️ README کامل، بدون عکس |


---

## ۶. باگ‌هایی که در همین بازبینی پیدا و رفع شدند

هیچ‌کدام در تودو نبودند. همه با **اجرا کردن** پیدا شدند، نه با خواندن کد.

| باگ | چه چیزی برای کاربر خراب بود |
|-----|------------------------------|
| `AdminPagination` کلید ترجمه‌اش را از فضای `admin` می‌گرفت ولی در `components/account/` هم استفاده می‌شد | صفحه‌ی **اعلان‌ها و تیکت‌های کاربر** به‌محض رسیدن به صفحه‌ی دوم با `MISSING_MESSAGE` سفید می‌شد |
| صفحه‌ی محصول کلید «سفارش بالای {amount} {currency}» را بدون پارامتر صدا می‌زد | در تضمین‌های خرید به‌جای مبلغ، خودِ `{amount}` نمایش داده می‌شد + خطای کنسول در **هر** صفحه‌ی محصول |
| `client.ts` روی پاسخ ۲۰۰ با بدنه‌ی خالی `SyntaxError` مهارنشده پرتاب می‌کرد | صفحه می‌افتاد و در لاگ فقط «Unexpected end of JSON input» می‌ماند |
| فرم خبرنامه ساختگی بود | «با موفقیت عضو شدید» می‌گفت و ایمیل را دور می‌ریخت |
| نشان «تأییدنشده» ایمیل بن‌بست بود | هشدار دائمی بدون هیچ راهی برای برطرف‌کردنش |
| اعلان تأیید، پیوند **نسبی** امضا می‌کرد ولی مسیر `signed` مطلق را می‌سنجید | هر پیوند تأیید «امضای نامعتبر» می‌گرفت — و **تست‌ها نگرفته بودند** چون خودشان پیوند را جور دیگری می‌ساختند |
| `InvalidSignatureException` پیام خام انگلیسی لاراول را نشان می‌داد | «Invalid signature.» وسط صفحه‌ی فارسی |
| دو دستور در `TODO-YOU.md` خراب بودند | `cd ..\nextstore-api` به دو خط شکسته و `..\tools\` به تب تبدیل شده بود — دستوری که کپی می‌کردید کار نمی‌کرد |

به‌علاوه ۹ سوئیت e2e که شکسته بودند: دو تای بالا باگ محصول بودند و بقیه
مسابقه‌ی زمانی (انتظار ثابت به‌جای شرط)، آلودگی داده بین اجراها، و یک
`STAMP` تعریف‌نشده. برای هر کدام محافظ گذاشته شد نه فقط وصله:
`scripts/lib/wait.mjs`، `artisan e2e:reset-limits`، بررسی سلامت پیش از
اجرا، و نگهبان نشتی فضای `admin` در `check:i18n`.

---

## ۷. نمره — به‌عنوان نمونه‌کار

### ۸.۵ از ۱۰

| محور | نمره | چرا |
|------|------|-----|
| **کیفیت کد** | ۹.۵ | PHPStan صفر خطا، بدون `any`، بدون `console.log`. توضیح‌ها **چرا** را می‌گویند نه *چه* — و اغلب باگ واقعی‌ای را که یک بار خورده شده مستند می‌کنند. این نادر است. |
| **تست** | ۹.۵ | ۲۵۳ تست بک‌اند + ۲۲ سوئیت مرورگر واقعی. مهم‌تر: تست‌ها **رفتار** را می‌سنجند نه پیاده‌سازی («سقف ساعتی با گذشتن دقیقه صفر نمی‌شود»). |
| **معماری** | ۹ | مرز سرویس/کنترلر/Resource تمیز. برچسب‌گذاری کش و باطل‌سازی هدفمند از پنل. رنگ و برچسب enumها از بک‌اند می‌آید تا فرانت نگاشت موازی نسازد. |
| **دوزبانگی و RTL** | ۱۰ | کامل، با نگهبان خودکار برابری کلیدها در CI. قیمت‌ها ریال ذخیره و در نمایش تبدیل می‌شوند. |
| **UX و جزئیات** | ۹ | هر چهار حالت داده پوشش دارد. پیام ۴۲۹ از خطای اعتبارسنجی جداست. رقم فارسی در `tel:` به لاتین تبدیل می‌شود — کسی این را نمی‌بیند مگر امتحان کند. |
| **پوشش دامنه** | ۷ | اینجاست که فاصله هست: تنوع محصول، کیف پول، گزارش‌ها، آپلود تصویر، و ورود با OTP/گوگل نیستند. یک فروشگاه واقعی بدون «رنگ و سایز» و بدون آپلود عکس، ناقص است. |
| **آمادگی تولید** | ۷.۵ | CI هست، مستندات API خودکار ساخته می‌شود، ایمیل‌ها در صف می‌روند. ولی درگاه شبیه‌سازی‌شده و مدیریت رسانه ندارد. |

### در یک جمله

**برای نشان‌دادن اینکه چطور کد می‌زنی، عالی است. برای نشان‌دادن اینکه
یک فروشگاه کامل ساخته‌ای، دو حفره دارد که به چشم می‌آید: تنوع محصول و
آپلود تصویر.**

### اگر فقط وقت یک کار را داری

**آپلود تصویر محصول.** الان مدیر محصول می‌سازد و نمی‌تواند عکس بگذارد —
و این چیزی است که هر کسی در سه دقیقه‌ی اول دیدن پنل امتحان می‌کند.
تنوع محصول بزرگ‌تر و چشمگیرتر است ولی نبودش را باید بدانی تا ببینی؛
نبودِ آپلود عکس را همان بار اول می‌بینی.
