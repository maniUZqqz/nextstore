<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

/**
 * اعلان‌های درون‌برنامه‌ای کاربر.
 * ---------------------------------------------------------------------------
 * ⚠️ چرا جدول اختصاصی و نه `notifications` خودِ لاراول؟
 *
 *    جدول پیش‌فرض لاراول کلید اصلی UUID و ستون `data` از نوع JSON دارد
 *    که کل محتوای اعلان را در خود نگه می‌دارد. برای اعلان ایمیلی خوب
 *    است، ولی اینجا سه چیز لازم داریم که با آن ساختار گران است:
 *
 *      ۱. فیلتر «خوانده‌نشده‌ها» با ایندکس — نه با جست‌وجو در JSON.
 *      ۲. پیوند مستقیم به سفارش یا تیکت، تا کلیک روی اعلان به همان
 *         صفحه برود.
 *      ۳. متن **دوزبانه**: کاربری که زبان سایت را عوض می‌کند باید
 *         اعلان‌های قدیمی‌اش را هم به زبان تازه ببیند.
 *
 *    ذخیره‌ی متنِ رندرشده در JSON یعنی اعلان‌های دیروز برای همیشه به
 *    زبان دیروز می‌مانند.
 */
return new class extends Migration
{
    public function up(): void
    {
        Schema::create('notifications', function (Blueprint $table) {
            $table->id();

            $table->foreignId('user_id')->constrained()->cascadeOnDelete();

            /*
             * نوع رویداد: order_status / ticket_reply / review_approved …
             *
             * ⚠️ متن اعلان از روی همین نوع ساخته می‌شود، نه ذخیره.
             *    (توضیح کامل در `App\Enums\NotificationType`)
             */
            $table->string('type', 40);

            /*
             * پارامترهای متن — نام محصول، شماره‌ی سفارش، وضعیت تازه.
             *
             * ⚠️ فقط **داده** اینجا می‌نشیند، نه جمله‌ی ساخته‌شده. جمله در
             *    زمان خواندن و به زبان درخواست ساخته می‌شود.
             */
            $table->json('data')->nullable();

            /*
             * مقصد کلیک — مسیر داخلی مثل «/account/orders/NS-260908-1234».
             *
             * ⚠️ رشته‌ی آزاد و نه کلید خارجی: اعلان ممکن است به سفارش،
             *    تیکت یا صفحه‌ی نظرات برود و هر کدام مدل دیگری‌اند.
             *    نگه‌داشتن نوع + شناسه یعنی هر مصرف‌کننده باید نگاشت
             *    خودش را داشته باشد.
             */
            $table->string('link', 255)->nullable();

            $table->timestamp('read_at')->nullable();

            $table->timestamps();

            /*
             * ⚠️ ایندکس ترکیبی، نه دو ایندکس جدا.
             *
             *    پرتکرارترین کوئری این جدول «اعلان‌های خوانده‌نشده‌ی این
             *    کاربر، تازه‌ترین اول» است — همان چیزی که نشان زنگوله در
             *    هر بارگذاری صفحه می‌خواند. ایندکس تک‌ستونی روی
             *    `user_id` مجبورش می‌کرد همه‌ی اعلان‌های کاربر را
             *    بخواند و بعد فیلتر کند.
             */
            $table->index(['user_id', 'read_at', 'created_at']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('notifications');
    }
};
