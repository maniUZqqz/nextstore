<?php

/**
 * پیکربندی قواعد کسب‌وکار فروشگاه
 * ===========================================================================
 * ⚠️ باگی که این فایل حل می‌کند:
 *
 *    اعداد هزینه‌ی ارسال در **سه جای مستقل** تعریف شده بودند:
 *
 *        OrderService   → 500_000 / 5_000_000   (منطق واقعی پرداخت)
 *        CartResource   → 500_000 / 5_000_000   (چیزی که سبد نشان می‌دهد)
 *        OrderSeeder    → 450_000 / 50_000_000  (داده‌ی نمایشی)
 *
 *    سیدر با دو تای دیگر نمی‌خواند: سفارش‌های نمونه هزینه‌ی ارسالی
 *    دارند که تسویه‌حساب واقعی هرگز تولید نمی‌کند. بدتر اینکه اگر
 *    روزی آستانه عوض شود، سبد خرید عددی نشان می‌دهد که پرداخت
 *    چیز دیگری می‌گیرد — و مشتری در لحظه‌ی پرداخت غافلگیر می‌شود.
 *
 *    پول نباید بیش از یک منبع حقیقت داشته باشد.
 *
 * همه‌ی مبالغ به **ریال** و عدد صحیح‌اند. هرگز اعشاری نه: محاسبات
 * پولی با float خطای گردکردن می‌سازد.
 */

return [

    'shipping' => [
        /*
         * آستانه‌ی ارسال رایگان — ۵۰۰ هزار تومان.
         * نوار بالای سایت هم همین عدد را تبلیغ می‌کند؛ اگر اینجا
         * عوض شد، متن آن نوار و صفحه‌ی «شیوه‌های ارسال» هم باید
         * به‌روز شوند.
         */
        'free_threshold' => 5_000_000,

        /** ارسال عادی — ۵۰ هزار تومان. بالای آستانه رایگان می‌شود. */
        'standard' => 500_000,

        /*
         * ارسال سریع — ۱۲۰ هزار تومان.
         * ⚠️ عمداً هرگز رایگان نمی‌شود، حتی بالای آستانه. تخفیف
         *    ارسال فقط روی روش عادی است.
         */
        'express' => 1_200_000,
    ],

    'tax' => [
        /** نرخ مالیات بر ارزش افزوده (۹ درصد). */
        'rate' => 0.09,
    ],

];
